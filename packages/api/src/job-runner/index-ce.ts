/**
 * Job-Runner: Process Job Collection (Community Mode)
 *
 * !note: need to consider multiple instances of server-api
 */

const publish = async (_: unknown): Promise<void> => {
  // FEATURE NOT SUPPORTED IN COMMUNITY-EDITION
};

/**
 * Start (or re-schedule) Job Runner
 */
const start = async (_?: unknown): Promise<void> => {
  // FEATURE NOT SUPPORTED IN COMMUNITY-EDITION
};

export default { start, publish };
